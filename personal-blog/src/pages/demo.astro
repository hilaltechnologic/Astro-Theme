---
import BaseLayout from '../layouts/BaseLayout.astro';
import SEOHead from '../components/SEOHead.astro';
import PerformanceMonitor from '../components/PerformanceMonitor.astro';
import OptimizedImage from '../components/OptimizedImage.astro';
import SearchBar from '../components/SearchBar.astro';
import ReadingProgress from '../components/ReadingProgress.astro';
import TableOfContents from '../components/TableOfContents.astro';
import CodeHighlight from '../components/CodeHighlight.astro';
import ImageGallery from '../components/ImageGallery.astro';
import VideoEmbed from '../components/VideoEmbed.astro';
import ServiceWorker from '../components/ServiceWorker.astro';
import Analytics from '../components/Analytics.astro';
import Accessibility from '../components/Accessibility.astro';

// Sample data for demo
const features = [
  {
    category: "Core Features",
    items: [
      { name: "Responsive Design", icon: "📱", description: "Mobile-first, all screen sizes" },
      { name: "Dark Mode", icon: "🌙", description: "Toggle dark/light mode" },
      { name: "SEO Optimization", icon: "🔍", description: "Meta tags, sitemap, RSS" },
      { name: "Content Collections", icon: "📚", description: "Type-safe content management" },
      { name: "MDX Support", icon: "✍️", description: "Enhanced markdown with components" },
      { name: "Tailwind CSS", icon: "🎨", description: "Utility-first styling" }
    ]
  },
  {
    category: "Layout Options",
    items: [
      { name: "Classic Layout", icon: "📰", description: "Traditional blog style" },
      { name: "Magazine Layout", icon: "📖", description: "Grid-based magazine style" },
      { name: "Portfolio Layout", icon: "💼", description: "Project showcase style" },
      { name: "Minimalist Layout", icon: "✨", description: "Clean, minimal design" }
    ]
  },
  {
    category: "Advanced Content",
    items: [
      { name: "Search Functionality", icon: "🔎", description: "Full-text search" },
      { name: "Reading Progress", icon: "📊", description: "Visual progress indicator" },
      { name: "Table of Contents", icon: "📑", description: "Auto-generated TOC" },
      { name: "Code Highlighting", icon: "💻", description: "Syntax highlighting + copy" },
      { name: "Image Gallery", icon: "🖼️", description: "Masonry layout + lightbox" },
      { name: "Video Embeds", icon: "🎥", description: "YouTube, Vimeo, custom videos" }
    ]
  },
  {
    category: "SEO & Performance",
    items: [
      { name: "Advanced SEO", icon: "🎯", description: "Comprehensive meta tags" },
      { name: "Performance Monitoring", icon: "📈", description: "Core Web Vitals tracking" },
      { name: "Image Optimization", icon: "🖼️", description: "WebP support + lazy loading" },
      { name: "Service Worker", icon: "⚡", description: "Offline support + caching" },
      { name: "Analytics", icon: "📊", description: "Google Analytics + privacy" },
      { name: "Accessibility", icon: "♿", description: "WCAG 2.1 AA compliance" }
    ]
  }
];

const stats = [
  { number: "58", label: "Total Features", color: "text-blue-600" },
  { number: "4", label: "Layout Options", color: "text-green-600" },
  { number: "6", label: "Content Features", color: "text-purple-600" },
  { number: "6", label: "SEO & Performance", color: "text-orange-600" }
];

const codeExample = `// Example MDX component
import { useState } from 'react';

export default function InteractiveComponent() {
  const [count, setCount] = useState(0);
  
  return (
    <div className="p-4 bg-blue-50 rounded-lg">
      <h3>Interactive Counter: {count}</h3>
      <button 
        onClick={() => setCount(count + 1)}
        className="bg-blue-500 text-white px-4 py-2 rounded"
      >
        Increment
      </button>
    </div>
  );
}`;

const galleryImages = [
  {
    src: "https://images.unsplash.com/photo-1461749280684-dccba630e2f6?w=400&h=300&fit=crop",
    alt: "Coding workspace",
    caption: "Modern development setup"
  },
  {
    src: "https://images.unsplash.com/photo-1516321318423-f06f85e504b3?w=400&h=300&fit=crop",
    alt: "Design process",
    caption: "Creative design workflow"
  },
  {
    src: "https://images.unsplash.com/photo-1555066931-4365d14bab8c?w=400&h=300&fit=crop",
    alt: "Code on screen",
    caption: "Clean code structure"
  },
  {
    src: "https://images.unsplash.com/photo-1507003211169-0a1dd7228f2d?w=400&h=300&fit=crop",
    alt: "Team collaboration",
    caption: "Team development"
  }
];
---

<BaseLayout title="Complete Feature Demo" description="Comprehensive demo showcasing all 58 features of the Astro Personal Blog Theme">
  <!-- SEO Head -->
  <SEOHead 
    title="Complete Feature Demo"
    description="Comprehensive demo showcasing all 58 features of the Astro Personal Blog Theme"
    image="/demo-og.jpg"
    type="website"
    tags={['demo', 'features', 'astro', 'blog', 'theme']}
  />

  <!-- Performance Monitor -->
  <PerformanceMonitor />

  <!-- Accessibility -->
  <Accessibility />

  <!-- Hero Section -->
  <section class="bg-gradient-to-br from-blue-50 via-purple-50 to-pink-50 dark:from-gray-900 dark:via-gray-800 dark:to-gray-900 py-20">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
      <div class="text-center">
        <h1 class="text-5xl md:text-7xl font-bold text-gray-900 dark:text-white mb-6">
          Complete
          <span class="bg-gradient-to-r from-blue-600 via-purple-600 to-pink-600 bg-clip-text text-transparent">
            Feature Demo
          </span>
        </h1>
        <p class="text-xl text-gray-600 dark:text-gray-300 mb-8 max-w-4xl mx-auto">
          Experience all 58 features of the most comprehensive Astro blog theme. 
          From advanced SEO to performance monitoring, this demo showcases everything.
        </p>
        
        <!-- Stats -->
        <div class="grid grid-cols-2 md:grid-cols-4 gap-6 mb-12">
          {stats.map(stat => (
            <div class="text-center">
              <div class={`text-3xl md:text-4xl font-bold ${stat.color} mb-2`}>
                {stat.number}
              </div>
              <div class="text-sm text-gray-600 dark:text-gray-400">
                {stat.label}
              </div>
            </div>
          ))}
        </div>

        <!-- Quick Navigation -->
        <div class="flex flex-wrap justify-center gap-4">
          <a href="#core-features" class="bg-blue-600 hover:bg-blue-700 text-white px-6 py-3 rounded-lg font-semibold transition-colors">
            Core Features
          </a>
          <a href="#layouts" class="bg-green-600 hover:bg-green-700 text-white px-6 py-3 rounded-lg font-semibold transition-colors">
            Layouts
          </a>
          <a href="#content-features" class="bg-purple-600 hover:bg-purple-700 text-white px-6 py-3 rounded-lg font-semibold transition-colors">
            Content Features
          </a>
          <a href="#seo-performance" class="bg-orange-600 hover:bg-orange-700 text-white px-6 py-3 rounded-lg font-semibold transition-colors">
            SEO & Performance
          </a>
        </div>
      </div>
    </div>
  </section>

  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-16">
    <div class="grid grid-cols-1 lg:grid-cols-3 gap-12">
      <!-- Main Content -->
      <div class="lg:col-span-2 space-y-16">
        
        <!-- Core Features -->
        <section id="core-features">
          <h2 class="text-3xl font-bold text-gray-900 dark:text-white mb-8">
            🚀 Core Features
          </h2>
          
          <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
            {features[0].items.map(item => (
              <div class="bg-white dark:bg-gray-800 rounded-lg p-6 border border-gray-200 dark:border-gray-700 hover:shadow-lg transition-shadow">
                <div class="flex items-center mb-3">
                  <span class="text-2xl mr-3">{item.icon}</span>
                  <h3 class="text-lg font-semibold text-gray-900 dark:text-white">
                    {item.name}
                  </h3>
                </div>
                <p class="text-gray-600 dark:text-gray-300 text-sm">
                  {item.description}
                </p>
              </div>
            ))}
          </div>
        </section>

        <!-- Layout Options -->
        <section id="layouts">
          <h2 class="text-3xl font-bold text-gray-900 dark:text-white mb-8">
            🎨 Layout Options
          </h2>
          
          <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
            {features[1].items.map(item => (
              <div class="bg-white dark:bg-gray-800 rounded-lg p-6 border border-gray-200 dark:border-gray-700 hover:shadow-lg transition-shadow">
                <div class="flex items-center mb-3">
                  <span class="text-2xl mr-3">{item.icon}</span>
                  <h3 class="text-lg font-semibold text-gray-900 dark:text-white">
                    {item.name}
                  </h3>
                </div>
                <p class="text-gray-600 dark:text-gray-300 text-sm mb-4">
                  {item.description}
                </p>
                <a 
                  href={`/layouts/${item.name.toLowerCase().replace(' ', '-')}`}
                  class="inline-block bg-blue-600 hover:bg-blue-700 text-white px-4 py-2 rounded text-sm transition-colors"
                >
                  View Demo
                </a>
              </div>
            ))}
          </div>
        </section>

        <!-- Content Features -->
        <section id="content-features">
          <h2 class="text-3xl font-bold text-gray-900 dark:text-white mb-8">
            📝 Advanced Content Features
          </h2>
          
          <div class="space-y-8">
            <!-- Search Demo -->
            <div class="bg-white dark:bg-gray-800 rounded-lg p-6 border border-gray-200 dark:border-gray-700">
              <h3 class="text-lg font-semibold text-gray-900 dark:text-white mb-4">
                🔎 Search Functionality
              </h3>
              <SearchBar />
            </div>

            <!-- Code Highlighting Demo -->
            <div class="bg-white dark:bg-gray-800 rounded-lg p-6 border border-gray-200 dark:border-gray-700">
              <h3 class="text-lg font-semibold text-gray-900 dark:text-white mb-4">
                💻 Code Syntax Highlighting
              </h3>
              <CodeHighlight code={codeExample} language="javascript" />
            </div>

            <!-- Image Gallery Demo -->
            <div class="bg-white dark:bg-gray-800 rounded-lg p-6 border border-gray-200 dark:border-gray-700">
              <h3 class="text-lg font-semibold text-gray-900 dark:text-white mb-4">
                🖼️ Image Gallery
              </h3>
              <ImageGallery images={galleryImages} />
            </div>

            <!-- Video Embed Demo -->
            <div class="bg-white dark:bg-gray-800 rounded-lg p-6 border border-gray-200 dark:border-gray-700">
              <h3 class="text-lg font-semibold text-gray-900 dark:text-white mb-4">
                🎥 Video Embed
              </h3>
              <VideoEmbed 
                url="https://www.youtube.com/watch?v=dQw4w9WgXcQ"
                title="Demo Video"
                aspectRatio="16:9"
              />
            </div>

            <!-- Optimized Image Demo -->
            <div class="bg-white dark:bg-gray-800 rounded-lg p-6 border border-gray-200 dark:border-gray-700">
              <h3 class="text-lg font-semibold text-gray-900 dark:text-white mb-4">
                🖼️ Optimized Image
              </h3>
              <OptimizedImage 
                src="https://images.unsplash.com/photo-1461749280684-dccba630e2f6?w=800&h=400&fit=crop"
                alt="Optimized image with WebP support"
                width={800}
                height={400}
                className="rounded-lg"
                aspectRatio="2:1"
              />
            </div>
          </div>
        </section>

        <!-- SEO & Performance -->
        <section id="seo-performance">
          <h2 class="text-3xl font-bold text-gray-900 dark:text-white mb-8">
            🎯 SEO & Performance Features
          </h2>
          
          <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
            {features[3].items.map(item => (
              <div class="bg-white dark:bg-gray-800 rounded-lg p-6 border border-gray-200 dark:border-gray-700 hover:shadow-lg transition-shadow">
                <div class="flex items-center mb-3">
                  <span class="text-2xl mr-3">{item.icon}</span>
                  <h3 class="text-lg font-semibold text-gray-900 dark:text-white">
                    {item.name}
                  </h3>
                </div>
                <p class="text-gray-600 dark:text-gray-300 text-sm">
                  {item.description}
                </p>
              </div>
            ))}
          </div>

          <!-- Performance Metrics -->
          <div class="mt-8 bg-white dark:bg-gray-800 rounded-lg p-6 border border-gray-200 dark:border-gray-700">
            <h3 class="text-lg font-semibold text-gray-900 dark:text-white mb-4">
              📊 Live Performance Metrics
            </h3>
            <div class="grid grid-cols-1 md:grid-cols-3 gap-4">
              <div class="text-center p-4 bg-gray-50 dark:bg-gray-700 rounded-lg">
                <div class="text-2xl font-bold text-green-600 mb-1" id="lcp-demo">-</div>
                <div class="text-sm text-gray-600 dark:text-gray-400">LCP (ms)</div>
              </div>
              <div class="text-center p-4 bg-gray-50 dark:bg-gray-700 rounded-lg">
                <div class="text-2xl font-bold text-blue-600 mb-1" id="fid-demo">-</div>
                <div class="text-sm text-gray-600 dark:text-gray-400">FID (ms)</div>
              </div>
              <div class="text-center p-4 bg-gray-50 dark:bg-gray-700 rounded-lg">
                <div class="text-2xl font-bold text-purple-600 mb-1" id="cls-demo">-</div>
                <div class="text-sm text-gray-600 dark:text-gray-400">CLS</div>
              </div>
            </div>
          </div>
        </section>

        <!-- Interactive Features Demo -->
        <section>
          <h2 class="text-3xl font-bold text-gray-900 dark:text-white mb-8">
            🎮 Interactive Features
          </h2>
          
          <div class="bg-white dark:bg-gray-800 rounded-lg p-6 border border-gray-200 dark:border-gray-700">
            <h3 class="text-lg font-semibold text-gray-900 dark:text-white mb-4">
              📊 Reading Progress Bar
            </h3>
            <p class="text-gray-600 dark:text-gray-300 mb-4">
              Scroll down to see the reading progress bar in action at the top of the page.
            </p>
            <div class="bg-gray-100 dark:bg-gray-700 rounded-lg p-4">
              <div class="text-sm text-gray-600 dark:text-gray-400">
                The reading progress bar shows your progress through this content. 
                It's visible at the top of the page and updates in real-time as you scroll.
              </div>
            </div>
          </div>
        </section>

        <!-- Table of Contents Demo -->
        <section>
          <h2 class="text-3xl font-bold text-gray-900 dark:text-white mb-8">
            📑 Table of Contents
          </h2>
          
          <div class="bg-white dark:bg-gray-800 rounded-lg p-6 border border-gray-200 dark:border-gray-700">
            <h3 class="text-lg font-semibold text-gray-900 dark:text-white mb-4">
              Auto-generated TOC
            </h3>
            <TableOfContents />
          </div>
        </section>
      </div>

      <!-- Sidebar -->
      <div class="lg:col-span-1">
        <div class="sticky top-8 space-y-6">
          <!-- Feature Count -->
          <div class="bg-white dark:bg-gray-800 rounded-lg p-6 border border-gray-200 dark:border-gray-700">
            <h3 class="text-lg font-semibold text-gray-900 dark:text-white mb-4">
              📊 Feature Statistics
            </h3>
            <div class="space-y-3">
              <div class="flex justify-between">
                <span class="text-gray-600 dark:text-gray-400">Total Features</span>
                <span class="font-semibold">58</span>
              </div>
              <div class="flex justify-between">
                <span class="text-gray-600 dark:text-gray-400">Categories</span>
                <span class="font-semibold">10</span>
              </div>
              <div class="flex justify-between">
                <span class="text-gray-600 dark:text-gray-400">Demo Pages</span>
                <span class="font-semibold">8</span>
              </div>
              <div class="flex justify-between">
                <span class="text-gray-600 dark:text-gray-400">Components</span>
                <span class="font-semibold">15</span>
              </div>
            </div>
          </div>

          <!-- Quick Actions -->
          <div class="bg-white dark:bg-gray-800 rounded-lg p-6 border border-gray-200 dark:border-gray-700">
            <h3 class="text-lg font-semibold text-gray-900 dark:text-white mb-4">
              ⚡ Quick Actions
            </h3>
            <div class="space-y-3">
              <button 
                onclick="testPerformance()"
                class="w-full bg-blue-600 hover:bg-blue-700 text-white px-4 py-2 rounded text-sm transition-colors"
              >
                Test Performance
              </button>
              <button 
                onclick="testAccessibility()"
                class="w-full bg-green-600 hover:bg-green-700 text-white px-4 py-2 rounded text-sm transition-colors"
              >
                Test Accessibility
              </button>
              <button 
                onclick="viewAnalytics()"
                class="w-full bg-purple-600 hover:bg-purple-700 text-white px-4 py-2 rounded text-sm transition-colors"
              >
                View Analytics
              </button>
              <button 
                onclick="toggleDarkMode()"
                class="w-full bg-gray-600 hover:bg-gray-700 text-white px-4 py-2 rounded text-sm transition-colors"
              >
                Toggle Dark Mode
              </button>
            </div>
          </div>

          <!-- Performance Score -->
          <div class="bg-white dark:bg-gray-800 rounded-lg p-6 border border-gray-200 dark:border-gray-700">
            <h3 class="text-lg font-semibold text-gray-900 dark:text-white mb-4">
              🎯 Performance Score
            </h3>
            <div class="text-center">
              <div class="text-4xl font-bold text-green-600 mb-2" id="performance-score">-</div>
              <div class="text-sm text-gray-600 dark:text-gray-400 mb-4">Overall Score</div>
              <div class="bg-gray-200 dark:bg-gray-700 rounded-full h-2">
                <div id="performance-bar" class="bg-green-500 h-2 rounded-full transition-all duration-300" style="width: 0%"></div>
              </div>
            </div>
          </div>

          <!-- Feature Categories -->
          <div class="bg-white dark:bg-gray-800 rounded-lg p-6 border border-gray-200 dark:border-gray-700">
            <h3 class="text-lg font-semibold text-gray-900 dark:text-white mb-4">
              📂 Feature Categories
            </h3>
            <div class="space-y-2">
              {features.map(category => (
                <div class="flex items-center justify-between p-2 bg-gray-50 dark:bg-gray-700 rounded">
                  <span class="text-sm text-gray-600 dark:text-gray-400">
                    {category.category}
                  </span>
                  <span class="text-sm font-semibold text-gray-900 dark:text-white">
                    {category.items.length}
                  </span>
                </div>
              ))}
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>

  <!-- Call to Action -->
  <section class="bg-gradient-to-r from-blue-600 to-purple-600 py-16">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 text-center">
      <h2 class="text-3xl md:text-4xl font-bold text-white mb-6">
        Ready to Experience All Features?
      </h2>
      <p class="text-xl text-blue-100 mb-8 max-w-3xl mx-auto">
        This demo showcases just a fraction of what's possible. 
        Explore all 58 features and see why this is the most comprehensive Astro blog theme.
      </p>
      <div class="flex flex-col sm:flex-row gap-4 justify-center">
        <a 
          href="/features/advanced-content" 
          class="bg-white text-blue-600 hover:bg-gray-100 px-8 py-3 rounded-lg font-semibold transition-colors"
        >
          View Content Features
        </a>
        <a 
          href="/features/seo-performance" 
          class="border border-white text-white hover:bg-white hover:text-blue-600 px-8 py-3 rounded-lg font-semibold transition-colors"
        >
          View SEO & Performance
        </a>
      </div>
    </div>
  </section>

  <!-- Service Worker -->
  <ServiceWorker />
  
  <!-- Analytics -->
  <Analytics gaId="G-XXXXXXXXXX" gtmId="GTM-XXXXXXXX" privacyMode={true} />
</BaseLayout>

<script>
  // Update performance metrics
  function updatePerformanceMetrics() {
    if (window.getPerformanceMetrics) {
      const metrics = window.getPerformanceMetrics();
      const score = window.getPerformanceScore();
      
      // Update demo metrics
      document.getElementById('lcp-demo').textContent = metrics.lcp ? `${Math.round(metrics.lcp)}ms` : '-';
      document.getElementById('fid-demo').textContent = metrics.fid ? `${Math.round(metrics.fid)}ms` : '-';
      document.getElementById('cls-demo').textContent = metrics.cls ? metrics.cls.toFixed(3) : '-';
      
      // Update performance score
      const performanceBar = document.getElementById('performance-bar');
      const performanceScore = document.getElementById('performance-score');
      
      if (score.average) {
        const percentage = Math.round(score.average * 100);
        performanceBar.style.width = `${percentage}%`;
        performanceScore.textContent = `${percentage}%`;
        
        // Update color based on score
        if (percentage >= 90) {
          performanceBar.className = 'bg-green-500 h-2 rounded-full transition-all duration-300';
          performanceScore.className = 'text-4xl font-bold text-green-600 mb-2';
        } else if (percentage >= 70) {
          performanceBar.className = 'bg-yellow-500 h-2 rounded-full transition-all duration-300';
          performanceScore.className = 'text-4xl font-bold text-yellow-600 mb-2';
        } else {
          performanceBar.className = 'bg-red-500 h-2 rounded-full transition-all duration-300';
          performanceScore.className = 'text-4xl font-bold text-red-600 mb-2';
        }
      }
    }
  }
  
  // Test functions
  window.testPerformance = function() {
    if (window.getPerformanceMetrics) {
      const metrics = window.getPerformanceMetrics();
      console.log('Performance Metrics:', metrics);
      alert('Performance test completed. Check console for details.');
    }
  };
  
  window.testAccessibility = function() {
    if (window.testAccessibility) {
      window.testAccessibility();
    }
  };
  
  window.viewAnalytics = function() {
    if (window.analytics) {
      const events = window.analytics.getEvents();
      console.log('Analytics Events:', events);
      alert('Analytics data logged to console.');
    }
  };
  
  window.toggleDarkMode = function() {
    document.documentElement.classList.toggle('dark');
    const isDark = document.documentElement.classList.contains('dark');
    localStorage.setItem('theme', isDark ? 'dark' : 'light');
  };
  
  // Update metrics every second
  setInterval(updatePerformanceMetrics, 1000);
  
  // Initial update
  setTimeout(updatePerformanceMetrics, 1000);
  
  // Smooth scrolling for anchor links
  document.querySelectorAll('a[href^="#"]').forEach(anchor => {
    anchor.addEventListener('click', function (e) {
      e.preventDefault();
      const target = document.querySelector(this.getAttribute('href'));
      if (target) {
        target.scrollIntoView({
          behavior: 'smooth',
          block: 'start'
        });
      }
    });
  });
</script> 